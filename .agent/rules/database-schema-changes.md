---
trigger: always_on
---

The D1 database is roughly an SQLite database.

When database schema changes are required, communicate the relevant schema change command to the user, but don't write some new throwaway markdown file - make sure to mention the schema change commands at the end of the walkthrough document, before testing.